---
title: "Guide Kyutai (Swift)"
description: "Exécuter le modèle vocal Kyutai sur macOS avec Swift et MLX."
category: "Guide"
---

# Kyutai sur Swift (MLX)

Ce guide explique comment faire tourner l'expérimentation Kyutai (Modèle Moshi/Mimi) localement sur un Mac Apple Silicon en utilisant Swift.

## Prérequis

1.  **Mac Apple Silicon** (M1/M2/M3).
2.  **Outils de ligne de commande Xcode** installés :
    ```bash
    xcode-select --install
    ```
3.  **Python 3.10+** (pour le téléchargement du modèle).

## Étape 1 : Télécharger le Modèle

Nous utilisons un script Python pour télécharger les poids du modèle (~2Go) correctement depuis HuggingFace.

Allez dans le dossier du lab :
```bash
cd lab/kyutai
```

Lancez le script de configuration (assurez-vous d'avoir les dépendances installées via `requirements.txt`) :

```bash
# Installer les dépendances si besoin
pip install -r requirements.txt

# Lancer le téléchargement
python kyutai_test.py
```

> [!NOTE]
> Les fichiers seront stockés dans le dossier `lab/kyutai/kyutai_model`.

## Étape 2 : Lancer avec Swift

Une fois le modèle téléchargé, nous utilisons une application Swift optimisée pour Metal (GPU).

Accédez au dossier de l'application Swift :
```bash
cd swift_app
```

Lancez simplement le programme :

```bash
swift run
```

### Ce que ça fait
1.  Compile le projet Swift et télécharge la librairie `mlx-swift`.
2.  Détecte votre puce Apple Silicon (GPU).
3.  Vérifie la présence des fichiers du modèle.
4.  Prêt pour l'inférence !

> [!SUCCESS]
> Si vous voyez "🚀 Initialisation de KyutaiRunner", c'est gagné !

## Dépannage

*   **Erreur "No such file"** : Vérifiez que vous avez bien fait l'étape 1 et que le dossier `kyutai_model` contient bien les fichiers `.safetensors`.
*   **Lenteur au premier lancement** : C'est normal, Swift doit récupérer et compiler les bibliothèques MLX.
